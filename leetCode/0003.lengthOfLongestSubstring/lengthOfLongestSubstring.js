/**
 * @param { string } s
 * @return { number }
 */
const lengthOfLongestSubstring = s => {
  if (s.length === 1) {
    return 1
  }

  for (let i = 0; i < s.length; i++) {
    for (let j = i + 1; j < s.length; j++) {

    }
  }
}
